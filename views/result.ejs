<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Result</title>
</head>
<body style="font-family: sans-serif; padding: 20px; background: #f0f0f0;">
    <h1>Thank You! <%= first %></h1>
    <p><strong>Name:</strong> <%= first %> <%= last %></p>
    <p><strong>Email:</strong> <%= email %></p>

    <h3>All Submissions:</h3>
    <ul>
        <% submissions.forEach(user => { %>
            <li style="list-style: none;"><strong><%= user.first%></strong> (Email: <%= user.email%>, Age: <%= user.age%>, phone: <%= user.phone%>)</li>
        <%})%>
    </ul>

    <a href="/">Back to form</a>
</body>
</html>